const converToSmallLetter = (str) => {
  return str.toLowerCase();
};

const converToCapitalLetter = (str) => {
  return str.toUpperCase();
};

module.exports = {
  converToSmallLetter,
  converToCapitalLetter,
};
